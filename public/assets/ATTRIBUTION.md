# Boss model assets

- `catbug.glb` is the project user's supplied `Meshy_AI_Ladybug_Kitty_0905011537_texture.glb`, copied
  with its authored textures. `evilcatbug.glb` is the existing project-user-supplied Evil Catbug asset.
  Both assets are granted free-to-use rights for this game. Authored materials and embedded JPEG
  textures are preserved unchanged at runtime; Catbug's runtime material clones only adjust surface
  response.
- `goose-hydra.glb` is the project user's supplied `Meshy_AI_The_Honking_Hydra_0905024240_texture.glb`,
  copied with its authored embedded textures. The project user grants free-to-use rights for this
  game; runtime clones preserve the supplied geometry and texture maps while normalizing surface
  response.
